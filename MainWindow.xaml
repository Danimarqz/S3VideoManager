<Window x:Class="S3VideoManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:S3VideoManager.Models"
        xmlns:chrome="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="S3 Video Manager"
        Width="1200"
        Height="720"
        MinWidth="1100"
        MinHeight="650"
        Background="{StaticResource WindowBackgroundBrush}"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Icon="pack://application:,,,/Resources/AppIcon.ico">
    <WindowChrome.WindowChrome>
        <chrome:WindowChrome CaptionHeight="0"
                             GlassFrameThickness="0"
                             CornerRadius="0"
                             ResizeBorderThickness="6"
                             UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <LinearGradientBrush x:Key="HeroGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1C2340" Offset="0" />
            <GradientStop Color="#0C1224" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="CardListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Card"
                                Background="#151C33"
                                CornerRadius="12"
                                Padding="14"
                                BorderBrush="#232D4C"
                                BorderThickness="1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#25336A" />
                                <Setter TargetName="Card" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Card"
                                Background="#151C33"
                                CornerRadius="12"
                                Padding="16"
                                BorderBrush="#232D4C"
                                BorderThickness="1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#25336A" />
                                <Setter TargetName="Card" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedDangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3A1C1C"/>
            <Setter Property="Foreground" Value="#FF6B6B"/>
            <Setter Property="BorderBrush" Value="#FF4545"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4A2C2C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A0C0C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <Grid.Style>
            <Style TargetType="Grid">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}" Value="Maximized">
                        <Setter Property="Margin" Value="8" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource TitleBarBrush}"
                BorderBrush="{StaticResource TitleBarBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,0"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <StackPanel Margin="10,0,0,0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <TextBlock Text="S3 Video Manager"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   Margin="0,5,6,0" />
                        <TextBlock Text="· Gestor HLS en S3"
                                   Foreground="#6F7AAA"
                                   FontSize="11"
                                   Margin="0,5,0,0" />
                    </StackPanel>
                </StackPanel>
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                    <Button Style="{StaticResource TitleBarButtonStyle}"
                            FontFamily="Segoe MDL2 Assets"
                            Click="MinimizeButton_Click"
                            Content="&#xE921;" />
                    <Button x:Name="MaximizeButton"
                            Style="{StaticResource TitleBarButtonStyle}"
                            FontFamily="Segoe MDL2 Assets"
                            Click="MaximizeButton_Click"
                            Content="&#xE922;" />
                    <Button Style="{StaticResource TitleBarCloseButtonStyle}"
                            FontFamily="Segoe MDL2 Assets"
                            Click="CloseButton_Click"
                            Content="&#xE8BB;" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1"
              Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Padding="20"
                    CornerRadius="18"
                    Background="{StaticResource HeroGradient}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="S3 Video Manager"
                                   Foreground="White"
                                   FontSize="28"
                                   FontWeight="Bold" />
                        <TextBlock Text="Administra materias y clases directamente en S3."
                                   Foreground="#A2B9FF"
                                   Margin="0,6,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button Content="Refrescar"
                                Command="{Binding RefreshSubjectsCommand}"
                                IsEnabled="{Binding IsWorking, Converter={StaticResource InverseBooleanConverter}}" />
                        <Button x:Name="ActivityButton"
                                Content="Ver actividad"
                                Margin="12,0,0,0"
                                Click="OpenActivityButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1"
                  Margin="0,24,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="360" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="{StaticResource PanelBackgroundBrush}"
                        CornerRadius="20"
                        Padding="24">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0"
                                   Text="Materias"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="White" />
                        <TextBlock Grid.Row="1"
                                   Text="Selecciona una para ver sus clases."
                                   Foreground="#7E8AAE"
                                   Margin="0,4,0,12" />
                        <Grid Grid.Row="2"
                              Margin="0,4,0,0">
                            <TextBox Height="34"
                                     Padding="10,4"
                                     VerticalContentAlignment="Center"
                                     Text="{Binding SubjectFilterText, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Filtra por nombre la lista de materias" />
                            <TextBlock Text="Busca la materia"
                                       Margin="12,0,0,0"
                                       Foreground="#6F7AAA"
                                       FontSize="12"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding SubjectFilterText, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />
                        </Grid>
                        <Button Grid.Row="3"
                                Content="+ Nueva materia"
                                Height="38"
                                Margin="0,4,0,0"
                                Click="NewSubjectButton_Click"
                                IsEnabled="{Binding IsWorking, Converter={StaticResource InverseBooleanConverter}}" />
                        <ListBox Grid.Row="4"
                                 x:Name="SubjectsList"
                                 Margin="0,16,0,0"
                                 ItemsSource="{Binding SubjectsView}"
                                 SelectedItem="{Binding SelectedSubject}"
                                 Background="{StaticResource HeroGradient}"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ItemContainerStyle="{StaticResource CardListBoxItemStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:SubjectModel}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   Foreground="White" />
                                        <TextBlock Text="Explora sus clases"
                                                   Foreground="#7581A6"
                                                   FontSize="12"
                                                   Margin="0,2,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <Grid Grid.Column="1"
                      Margin="24,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Clases"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal">
                            <Button Content="Subir clase"
                                    Width="150"
                                    Margin="0,0,10,0"
                                    Click="UploadClassButton_Click"
                                    IsEnabled="{Binding CanStartTransfer}" />
                            <Button Content="Eliminar clase"
                                    Width="150"
                                    Click="DeleteClassButton_Click"
                                    IsEnabled="{Binding SelectedClass, Converter={StaticResource NullToBooleanConverter}}" />
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1"
                            Background="{StaticResource PanelBackgroundBrush}"
                            CornerRadius="20"
                            Padding="20"
                            Margin="0,8,0,0">
                        <ListView ItemsSource="{Binding Classes}"
                                  SelectedItem="{Binding SelectedClass}"
                                  ItemContainerStyle="{StaticResource CardListViewItemStyle}"
                                  BorderThickness="0"
                                  Background="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:ClassModel}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}"
                                                           FontSize="16"
                                                           FontWeight="SemiBold"
                                                           Foreground="White" />
                                                <TextBlock Text="{Binding Prefix}"
                                                           Foreground="#7A88AF"
                                                           FontSize="12" />
                                            </StackPanel>
                                            <Button Grid.Column="1"
                                                    Content="Copiar etiqueta"
                                                    Margin="0,0,12,0"
                                                    Padding="10,4"
                                                    Click="CopyClassLabelButton_Click"
                                                    VerticalAlignment="Center" />
                                        </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>
            </Grid>

            <Grid Grid.Row="2"
                  VerticalAlignment="Center"
                  Margin="0,16,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="#A6B4DA"
                           VerticalAlignment="Center"
                           FontSize="14"
                           TextTrimming="CharacterEllipsis" />

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Content="Cancelar"
                            Command="{Binding CancelTransferCommand}"
                            Visibility="{Binding TransferInProgress, Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="0,0,24,0"
                            Style="{StaticResource RoundedDangerButtonStyle}"
                            MinWidth="120" />

                    <Grid Width="280"
                          VerticalAlignment="Center">
                        <ProgressBar Height="16"
                                     Minimum="0"
                                     Maximum="1"
                                     Value="{Binding OperationProgress}"
                                     Visibility="{Binding TransferInProgress, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <TextBlock Text="{Binding OperationProgress, StringFormat={}{0:P0}}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="Black"
                                   Visibility="{Binding TransferInProgress, Converter={StaticResource BoolToVisibilityConverter}}"
                                   IsHitTestVisible="False"/>
                    </Grid>
                </StackPanel>
            </Grid>

            <Border Grid.RowSpan="3"
                    Background="#88090F25"
                    Visibility="{Binding IsWorking, Converter={StaticResource BoolToVisibilityConverter}}">
                <Border HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="#151C33"
                        Padding="24"
                        CornerRadius="16"
                        Width="260">
                    <StackPanel>
                        <ProgressBar IsIndeterminate="True" Height="6" />
                        <TextBlock Text="Conectando..."
                                   HorizontalAlignment="Center"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   Margin="0,12,0,0" />
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Grid>
</Window>
